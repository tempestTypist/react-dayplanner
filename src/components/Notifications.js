import { Card } from 'react-bootstrap';

const Notifications = () => {
  return (
		<Card>
			<div className="notification-container softcard-container">
				<h2>Notifications</h2>
			</div>
		</Card>
  );
};

export default Notifications;